VERDICT: APPROVED

# Review — Abuse Reports (submission path)

Reviewed against `.process/abuse-reports/01-plan.md`, `.process/abuse-reports/02-implementation.md`,
the vendored `.claude/skills/dotnet-feature/SKILL.md`, `conventions/dotnet-testing.md`,
`conventions/react-feature.md` and `.claude/rules/docs-sync.md`.
All 22 created and 17 modified files read end to end. All five gates independently re-run.

## Blocking

None.

## Non-blocking

- `api/E3A.Tests/Reports/SubmitReport/SubmitReportHandlerTests.cs:1-129` — 129 lines, over the skill §1 "~80–100" rule and testing §10. Not gating: the rule is written with a tilde, the implementer already compacted it from 138, and the plan (test plan rows 4–9) fixes the class name `SubmitReportHandlerTests`, so a split would have had to invent a class name the contract did not authorise. Refusing to deviate unilaterally was the right call. If a split is wanted later, tests 6–8 form a natural `...ReporterTests` group.
- `postman/e3a.postman_collection.json:687` — the example body ships `"itemId": "00000000-0000-0000-0000-000000000000"`, which `SubmitReportValidator.cs:16` rejects, so pressing Send always returns `422 REPORT_ITEM_ID_REQUIRED` and never exercises the handler. The request is not missing, stale or orphaned — URL, method, `"auth": {"type":"noauth"}`, header and every field name and enum value mirror the shipped contract — so this is not a Postman-sync gap. It is a weak example. Swapping in a real catalog id would make it demonstrate the endpoint rather than only document it. The plan prescribed the value verbatim; the implementer flagged it rather than changing it silently, which is correct.
- `api/E3A.Api/appsettings.json` (gitignored at `.gitignore:23`) — the `"Reports"` section exists on disk (verified: `DetailsMaxLength: 1000`, `MaxReportsPerItem: 20`, placed after `"Catalog"`) but cannot be part of this change. **This is pre-existing repo behaviour, not a defect of this slice**: I confirmed `Engineers`, `Catalog`, `GitHubAuthentication` and the rest live in the same untracked file, so every `[Area]Options` section is equally absent from a fresh checkout — a fresh checkout is already broken before `Reports` is added. Recommendation for the dev: a separate slice adds a tracked `appsettings.Example.json` (or a tracked `appsettings.json` with secrets moved to user secrets / App Configuration). Do not let this slice carry a repo-wide config-tracking change.
- `web/src/app/ReportContext.tsx:43` — closing the modal via `ModalOverlay onClose={close}` while a submit is in flight means a later failure sets `errorMessage` on a closed modal and the user sees nothing. The plan explicitly reasoned about the absence of a `cancelled` guard, and the submit button is disabled while `submitting`, so the window is narrow. Noting, not gating.

## Verified

Claims from `02-implementation.md` independently confirmed:

- `dotnet build api/E3a.slnx` → **Build succeeded. 9 Warning(s) 0 Error(s)**; all 9 are in `api/core-libraries/` (Core.Validation CS8602 x2, Core.OTP CS8618 x2, Core.Notifications CS8618 x5). Zero warnings from any file in this change.
- `dotnet test api/E3A.Tests/E3A.Tests.csproj` → **Failed: 0, Passed: 798, Skipped: 0**.
- `npm run build` (`tsc -b && vite build`) → zero TypeScript errors, built in 132ms.
- `npm run test` → **Test Files 11 passed / Tests 69 passed**.
- `npx oxlint` → 8 warnings. Baseline re-measured myself from `git archive HEAD web` into a scratch directory: **the same 8**, same files, same rules; the only difference is `ReportContext.tsx` `only-export-components` moving from `52:17` to `69:17`. Zero new warnings, zero errors. No `oxlint-disable`, `@ts-ignore` or `ts-expect-error` in `web/src`.
- **Decision 8 (the load-bearing one) — confirmed where it is observable.** `ReportsController.cs:10` carries class-level `[Authorize]` and `ReportsController.cs:14` carries action-level `[AllowAnonymous]`, exactly as planned. `Program.cs` has `app.UseAuthorization()` at line 105 and **no** `app.UseAuthentication()`, so the plan's premise is factually correct. I queried the local `Reports` table myself:

  | Id | ItemType | Reason | Status | ReporterUserId | CreatedBy |
  |---|---|---|---|---|---|
  | 81FA7F4C... | Engineer | Other | Open | 57E40D05-...-6828405F8DA0 | 57E40D05-...-6828405F8DA0 |
  | ADCA8671... | Engineer | Spam | Open | NULL | NULL |
  | E03E8717... | Engineer | Malicious | Open | NULL | NULL |

  The bearer-carrying POST stored a real `ReporterUserId` **and** `CreatedBy`; the anonymous POSTs stored NULL, not `Guid.Empty`. **The positive evidence is sufficient.** The un-run counterfactual (bare `[AllowAnonymous]` yielding NULL for a bearer caller) would only have shown that `[Authorize]` is *necessary*; it would not have changed the fact that the shipped code is *correct*, which the attributed row establishes directly. The definition of done asks for exactly this row inspection. Declining to route around the permission denial was the right call.
- **Deviation 1** (`[property: JsonRequired]` on `ItemId`) — the right remedy: it is `S6964`'s own first-listed fix, the same idiom the plan already applied to both enums in that record, and the minimal change; making `ItemId` a `Guid?` would have pushed a nullable into the command for an analyzer's sake. It **does** change the wire contract in one narrow way, disclosed accurately: an *omitted* `itemId` now fails deserialization (400) instead of reaching the validator (422). An all-zeros `itemId` still reaches `SubmitReportValidator.cs:16` and still yields `422 REPORT_ITEM_ID_REQUIRED`, so no error code is orphaned and no doc claim is contradicted — `docs/implementation-plan.md` §API surface enumerates only 400 and 429 for this route.
- **Deviation 3** (`_ = ...` for mutation 3) — sound, and it does not weaken the mutation. Both repository calls still execute, so the mutant isolates exactly the deleted guard; deleting the whole block instead would have failed test 5's `Received(1).GetByIdAsync` for an unrelated reason and muddied the signal.
- The three mutation outcomes are consistent with the code I read. `>=` → `>`: test 12 stubs `CountAsync` = 3 = `MaxReportsPerItem` (`SubmitReportHandlerGuardTests.cs:63`) while test 9 stubs 2 (`SubmitReportHandlerTests.cs:100`), so exactly test 12 flips. Ternary deleted: every other handler test leaves `_currentUserService.UserId` unstubbed (NSubstitute returns `null` for `Guid?`) or stubs a real Guid, so only test 8 flips. `throw` deleted: only tests 10 and 11 stub a missing item, so only those two flip. All three match the reported output.
- The report's admission that **the step-4 `CountAsync` predicate is constrained by nothing** is accurate. Both call sites use `Arg.Any<Expression<Func<Report, bool>>?>()` (`SubmitReportHandlerTests.cs:100`, `SubmitReportHandlerGuardTests.cs:63`); NSubstitute never invokes the expression, so replacing `x.ItemType == request.ItemType && x.ItemId == request.ItemId` with `x => true` would leave the suite green. **No test in this change claims otherwise** — I checked every assertion in all four backend test classes. This is `conventions/dotnet-testing.md` §9's second trap, correctly named rather than papered over.

Plan items confirmed present and correct:

- Every file in *Files to create* exists (22/22) and **nothing extra was created** — `git status --untracked-files=all` shows only those 22 plus `.process/abuse-reports/`. No doc was created outside `/docs`. No stray `api-run.log` and no `.gitignore` edit survives.
- `Report.cs:6-31` — `AuditEntity`, private ctor, single `static Create`, no public setters, no `Delete()` or transition method, `IsAnonymous` inline. `ReportStatus.cs:3-6` has **exactly one** member. `IReportRepository.cs:5` is empty. No new exception type, service, repository method or abstraction was invented.
- Zero numeric literals for lengths or counts in `Report.cs`, `SubmitReportValidator.cs` or `ErrorCodes.cs`; all caps come from `ReportsOptions` (`SubmitReportValidator.cs:14`, `AppDbContext.cs:129`).
- `SubmitReportHandler.cs` — existence guard at line 27 **before** the cap at line 34; exactly one `SaveChangesAsync` (line 43) and only on the success path; no `try`/`catch`; no `UnauthorizedCoreException`; `Guid.Empty` normalised to `null` at line 39 before `Report.Create`; `.ConfigureAwait(false)` on all five awaits; one-line class declaration; `CountAsync(cancellationToken, predicate)` argument order matches `IRepository<T>` (`Core.DDD/Repositories/IRepository.cs:35`).
- No status oracle: the switch at `SubmitReportHandler.cs:21-25` calls `GetByIdAsync` with no status predicate, so `Draft`, `Published` and `Unlisted` produce identical outcomes. The only filter in play is the global soft-delete one at `AppDbContext.cs:153`.
- `AppDbContext.cs` — ctor param at line 18, `DbSet<Report>` at line 30, `ConfigureReports(modelBuilder)` at line 40, the private method at lines 127-140, the global soft-delete line at 153. No `.Where(!IsDeleted)` anywhere in the slice (§8.5 honoured). `IReportRepository` registered in `AddInfrastructure` alphabetically after `IItemVersionRepository`.
- Migration `20260904220511_reports006.cs` — `Reports` table with three `nvarchar(50)` enum columns, `Details nvarchar(1000) NULL`, `ReporterUserId uniqueidentifier NULL`, every `AuditEntity`/`Entity` column, non-unique `IX_Reports_ItemType_ItemId` and `IX_Reports_ReporterUserId`, no unique index, no FK. `AppDbContextModelSnapshot.cs` diff is purely additive and tool-shaped.
- All 7 error codes in `ErrorCodes.cs:112-118`, `Messages.en.resx` and `Messages.ar.resx`; `{limit}` present in **both** language values of `REPORT_LIMIT_REACHED`; Arabic without tashkeel.
- `postman/e3a.postman_collection.json:670-708` — `Reports / Submit Report`, `POST`, `{{baseUrl}}/api/reports` with the correct `host`/`path` split, `Content-Type: application/json`, `"auth": {"type": "noauth"}`, folder placed after `Catalog` (line 581) and before `Publishing` (line 709). No stale or orphaned report request anywhere in the collection.
- Frontend, judged against `conventions/react-feature.md` (not the .NET skill): `reportsApi.ts` is a thin `requestJson` wrapper with named exports only and `import type` where needed; `ReportContext.tsx:34` calls `submitReport`, never `fetch`; the toast fires **only** inside `.then` (line 35); failure renders `messageForApiError(error)` at line 58 and leaves the modal open; submit is `disabled={submitting || !canSubmitReport(reason, details)}` (line 61); both action buttons and the close control carry `type="button"`, the close carries `aria-label="Close"`, and the reason control is a `<select>` — the `<div onClick>` and `<span onClick>` are gone (§6 satisfied). All 7 report codes are in `errorMessages.ts:23-29`. `reportReasons` is gone from `catalog.ts`, and `grep -rn "reportReasons|useReport" web/src` returns only the live `useReport` export and its one remaining consumer, `EngineerDetailPage.tsx:7,16` — **no dead `useReport` import survives in `Footer.tsx` or `DetailHeader.tsx`**, and `tsc -b` passing with `noUnusedLocals` is the mechanical proof.
- **Docs sync — no divergence.** `docs/implementation-plan.md` §Data model (`reports` bullet), §Key architecture decisions → Current item 4, and §API surface `Social:` clause now all describe what the code does: `Status(Open)` with moderation deferred, `Details` capped by `ReportsOptions.DetailsMaxLength`, both non-unique indexes, no FK, no per-reporter uniqueness, the in-API `MaxReportsPerItem` cap returning 429 `REPORT_LIMIT_REACHED` alongside Cloudflare still owned by P6, and the engineer-detail-only / team-deferred scope. `docs/security-scan.md` §Outcomes no longer implies a working end-to-end backstop — it now states plainly that pulling a reported item is a **manual operator action** with no moderation UI and no automated takedown in v0.1, which is exactly what the code delivers. `docs/architecture.md` enumerates no tables and no endpoints (checked), so it correctly needed no edit. `docs/design-prompt.md:27` ("Report link" on engineer detail) and `:36` ("reason dropdown + details textarea") now match the code more closely than before; `:28` ("Team detail — same layout") describes a target the team catalog cannot yet serve, which is **incompleteness, not divergence** — and the scope deferral itself is recorded in `implementation-plan.md` §API surface, the doc that owns scope. `:25` names the footer but enumerates no links, so removing "Report abuse" contradicts nothing.
- Anonymous write on `POST /api/reports` is not a SKILL §7.3 violation to hold against this slice: `docs/implementation-plan.md` §Current 4 has always specified anonymous reports, there is no `DefaultCodes` class in this repo (verified), and the controller mirrors the existing `AuthenticationController` shape.

## Test quality

All 30 planned tests exist with the exact planned class and method names (backend 1–19, frontend 20–30). Frontend count reconciles: 58 baseline + 11 = 69.

- **`ReportTests`** (1–3) — constrains. Test 1 pins `Open`, the argument round-trip and both date stamps against a captured `before`; tests 2 and 3 pin the `ReporterUserId` / `CreatedBy` / `IsAnonymous` triple in both directions. Deleting `Status = ReportStatus.Open` or the `reporterUserId` argument to the private ctor fails them.
- **`SubmitReportHandlerTests`** (4–9) — constrains. `Arg.Is<Report>(...)` inside `Received(1).AddAsync` is a *received-call* matcher, which NSubstitute genuinely invokes, so these are real assertions and not the stubbed-predicate trap. Test 5 pins the switch direction with `_engineerRepository.DidNotReceive().GetByIdAsync(...)`; test 4 pins it from the other side implicitly, since only the engineer repository is stubbed and a swapped arm would 400. Test 9 is the lower half of the `>=` boundary and mutation 1 showed it holding while test 12 flipped.
- **`SubmitReportHandlerGuardTests`** (10–12) — constrains. Every `throw` in the new code has a test; each asserts the `ErrorCodes.*` constant (never a message string) plus `DidNotReceive()` on both `AddAsync` and `SaveChangesAsync`. Test 12 also pins `Context["limit"]`, so the 429 body cannot drift silently.
- **`SubmitReportValidatorTests`** (13–19) — constrains. One passing fact plus one failing case per rule, with the `Other`-requires-details rule covered as a `[Theory]` over `null` / `""` / `"   "`. Test 18 derives the boundary from `ReportFactory.CreateReportsOptions().DetailsMaxLength` rather than a literal, so it tracks the configured cap.
- **`reportsApi.test.ts`** (20–30) — constrains. Tests 22 and 23 are the ones that matter for this slice's anonymous-capable premise and they bite in both directions (no header signed out, `Bearer jwt` signed in). Test 30 is the real frontend/server enum sync check. Test 24 uses `.catch(caught => caught)` instead of the plan's `rejects.toBeInstanceOf` phrasing — same assertion semantics, and it still fails if `submitReport` resolves.
- **What constrains nothing, correctly disclosed:** the `CountAsync` predicate at `SubmitReportHandler.cs:32` and the `GetByIdAsync` include / `asNoTracking` arguments. No test claims them. `ReportsController`, `ReportRepository`, `ConfigureReports` and the migration are out of scope by `conventions/dotnet-testing.md` §5; the controller's attribute pair is evidenced instead by the database rows above, which is the only place it is observable.
