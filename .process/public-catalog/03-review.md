VERDICT: APPROVED

# Review — Anonymous Public Catalog

## Blocking
None.

## Non-blocking
- `api/E3A.Application/Catalog/Shared/CatalogEngineerResultGenerator.cs:17` — the `Deserialize<ImportManifestResult>(json)!` path can 500 an anonymous endpoint on malformed `DraftManifestJson`. Judged non-blocking: the only writer of that column is `UploadEngineerDraftHandler`'s own serializer, so no request-reachable input produces the failure — it requires out-of-band DB tampering or a future shape change. Plan Decision 8 mandated the verbatim mirror and was gate-approved. Slice ③ (switch to `FrozenManifestJson`) must keep the shape compatible or add a guard then.
- `api/E3A.Tests/Engineers/EngineerTests.cs` — now 151 lines, over the ~100 guidance (was 130 before this slice; the plan mandated appending, not splitting). Split by behaviour group in a future slice.
- `api/E3A.Application/Catalog/GetCatalog/GetCatalogQueryHandler.cs:52` — if the `Catalog` config section is absent (the file is gitignored), `DefaultPageSize` binds to 0 and `TotalPages = (long)Math.Ceiling(count / 0.0)` is garbage rather than a loud failure. Same unvalidated-options pattern as every existing `[Area]Options`; a repo-wide options-validation pass would be the fix, not this slice.

## Verified
- **Build**: re-ran `dotnet build E3A.slnx` — `Build succeeded`, 9 warnings, 0 errors; all 9 originate in `core-libraries` (CS8602 ×2 Core.Validation, CS8618 ×2 Core.OTP, ×5 Core.Notifications), zero from `E3A.*`. Matches the report exactly.
- **Tests**: re-ran `dotnet test E3A.Tests --no-build` — 166/166 passed, 0 skipped. All 36 planned test methods exist with the exact plan names (34 under `E3A.Tests/Catalog`, 2 appended to `EngineerTests`); the 3 `ListEngineersQueryHandlerTests` cases are the only removals.
- **Declared deviation (gitignored appsettings)**: confirmed `.gitignore:23` ignores `api/E3A.Api/appsettings.json`; the `Catalog` section exists on disk with the plan's verbatim values (9/50/100/10/30). Pre-existing, dated repo policy (constitution §2); correctly declared, correctly applied locally, correctly flagged for Azure App Configuration provisioning. Not a hidden deviation.
- **File inventory**: exactly 14 production files + 1 controller + 2 postman + 7 test files at the plan's exact paths; no extra files; every signature, namespace and type name matches the plan's contracts verbatim (compared file by file). `IEngineerRepository`, `Program.cs`, `E3A.Infrastructure/**`, all `.csproj` untouched.
- **Decision 1 (list removal)**: `Engineers/ListEngineers/` and its tests deleted; `EngineersController` diff removes only the action + `using`; zero remaining `ListEngineers` references anywhere in `api/`, `docs/`, `web/`, `postman/`.
- **Handlers**: all three match the plan's step lists exactly — published-only `FindAsync(asNoTracking: true)`, in-memory filter/sort/page, hand-built `PageData`; detail 404s drafts via `Slug && Published` predicate throwing `NotFoundCoreException(EngineerNotFound)` (reused, not duplicated); tags lowercased, engineer-level counts, count desc then ordinal tag asc. No `SaveChangesAsync`, no `ICurrentUserService`, no `try`/`catch`, `.ConfigureAwait(false)` on every await.
- **Skill §9 + §8 walk**: file-scoped namespaces, sealed records/classes, one-line declarations, `[]` collections, `DateTimeOffset` only, no comments, no magic values (all five tunables in `CatalogOptions` bound in `AddApplication`) — §8.1 honoured; §8.2/8.3/8.4 have no subject in this slice; §8.5 clean (both queries filter `Status == Published` only, no ad-hoc `IsDeleted`).
- **Error codes**: 7 new constants with exact values in `ErrorCodes.cs` under `// Catalog`; all 7 keys present in both `Messages.en.resx` and `Messages.ar.resx` (Arabic without tashkeel).
- **Postman sync (Review order #7)**: both files parse as JSON (`json.load`); v2.1 schema URL; collection-level bearer `{{token}}`; Engineers folder has exactly the 7 real authenticated/anonymous Engineers endpoints (POST create, GET mine, GET {id}, PUT, POST upload formdata, GET import-manifest, DELETE) with correct methods/URLs/bodies; Catalog folder has the 3 routes each with request-level `noauth`; NO request for the removed `GET /api/engineers` list; environment `baseUrl = https://localhost:62935` matches `launchSettings.json:9`.
- **Docs sync (Review order #8)**: both planned edits landed verbatim — `docs/implementation-plan.md` API-surface line now routes the anonymous list to `/catalog` and adds `/catalog/tags` + `pageSize`; `docs/architecture.md` Principles clause now names catalog browse. Grepped `/docs` for stale references to the removed list — none. `type` remaining in the catalog line is target-state (teams, slice ④) — incompleteness, not divergence.
- **Report attention items 1, 2, 5**: FilterTests restructure keeps all 8 plan tests (98 lines) and the by-`Id` selection makes the mapping assert order-independent — an improvement; fixture wording isolates one match field per search test while asserting exactly the plan's behaviour rows; route precedence left to framework ranking per gate-approved Decision 15.

## Test quality
- `GetCatalogQueryHandlerFilterTests` — genuinely constraining: predicate-compilation check (test 14) proves the repository filter admits published and rejects drafts; each search/tag test has a non-matching fixture that would surface a broken predicate; the trim test's double-space input only matches if trimming actually happens.
- `GetCatalogQueryHandlerPagingTests` — constraining: distinct slugs pin order for both sorts and the tie-break; the page-2 test asserts all five `PageData` members; the null-page-size test uses `DefaultPageSize = 2` against 3 rows so the default is load-bearing.
- `GetCatalogTagsQueryHandlerTests` — constraining: casing-merge, per-engineer dedupe, and count-then-ordinal ordering each fail under the obvious wrong implementations (occurrence counting, case-sensitive grouping, unordered output).
- `GetCatalogEngineerQueryHandlerTests` — constraining: the three-way predicate check (published+slug true, draft same slug false, published other slug false) is the strongest possible assertion through a substitute; not-found path asserts on `ErrorCodes.EngineerNotFound`.
- `CatalogEngineerResultGeneratorTests` — constraining: real round-trip through `JsonSerializer.Serialize`/`ReplaceDraftManifest`, no substitute-echo.
- Validator tests — one failing case per rule asserting the exact `ErrorCodes.*` constant, plus two passing cases. No vacuous tests found anywhere in the slice.
